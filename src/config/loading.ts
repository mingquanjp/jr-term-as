/** Intro shown once when the application first opens. */
export const OPENING_SPLASH_DURATION_MS = 3800

/** Shinkansen loading screen shown after a successful login. */
export const LOGIN_LOADING_DURATION_MS = 3000
